T = int(input())
# Given multiple test cases, we handle each one.
for test_case in range(1, T + 1):
    group = list(map(int, input().split()))
    answer = 0
    for i in group:
        if i % 2 != 0:
            answer += i
    print('#' + str(test_case), str(answer))
